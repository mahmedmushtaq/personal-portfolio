import { worksData } from './../../data/works';

const initialState = {
  worksData,
};

export default (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

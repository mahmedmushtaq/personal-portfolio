import { skillsPageData } from './../../data/skills';

const initialState = {
  skillsPageData,
};

export default (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};
